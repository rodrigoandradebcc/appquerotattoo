import React from 'react';
import { Text } from 'react-native';

import { Container } from './styles';

const Menu: React.FC = () => {
  return (
    <Container>
      <Text>Menu</Text>
    </Container>
  );
};

export default Menu;
